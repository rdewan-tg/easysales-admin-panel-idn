
import { Box } from "@mui/material";
import { Outlet } from "react-router-dom";

const SalesHeaderScreen = () => {
  return (
    <Box>
      <Outlet />
    </Box>
  );
};

export default SalesHeaderScreen;
